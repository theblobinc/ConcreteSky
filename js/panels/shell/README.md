# Panel Shell

`bsky-panel-shell` is the shared container used by panel components.

It defines:
- the canonical scroller used for infinite-scroll + intersection observers
- a consistent title/toolbar header layout
- density/padding conventions
