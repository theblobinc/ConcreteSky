# Bars

Bars are persistent UI components that live alongside the panel system (for example, notification bar overlays).

They usually mount at the app-shell level and often render entry-like items, so they should reuse `js/panels/entries/*` utilities.

Related docs:

- Panel system contract: `PANELS_API.md`
- Entry primitives (lazy media/mount): `PANELS_ENTRIES.md`
